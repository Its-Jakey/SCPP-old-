#include <graphics>
#include <strings>


namespace demo;

    public func main() {
        for (i from 0 to 10) {
            graphics::drawString(strings::join("hello world #", i));
            graphics::newLine();
        }
        graphics::flip();
    }

endNamespace;
